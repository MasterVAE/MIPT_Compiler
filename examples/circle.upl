a = 0;
b = 0;

while(a < 10)
{
    b = 0;
    while(b < 10)
    {
        if((a - 5) * (a - 5) + (b - 5) * (b - 5) < 20)
        {
            set(a, b);
        };
        b = b + 1;
    };

    a = a + 1;
};

draw;